name: Keep Render App Awake

on:
  schedule:
    # Runs every 10 minutes
    - cron: '*/12 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -sSf "${{ secrets.RENDER_APP_URL }}"
